# Database Connection
# NOTE: This file is for reference only. 
# The actual DATABASE_URL should be set in the root .env file at the monorepo level.
# Prisma will automatically read from the root .env file.

DATABASE_URL="postgresql://user:password@localhost:5432/org_inventory"

# Optional: For development
NODE_ENV="development"
